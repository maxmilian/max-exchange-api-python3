#!/usr/bin/env python
from setuptools import setup

setup(
    name = 'max-exchange-api-python3',
    packages = ['max'],
    version = '0.1',
    description = 'Python API for MAX exchange',
    author = 'maxmilian',
    author_email = '',
    url = 'https://github.com/maxmilian/max-exchange-api-python3',
    download_url = '',
    keywords = [],
    classifiers = [],
    license='MIT',
    install_requires=[],
    entry_points={},
    zip_safe=True
)
